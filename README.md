Updated images to be software/QA related using Unsplash source images and ensured the header logo points to /assets/logo.svg (previously the page referenced a missing logo.png). If you want me to upload a raster logo (webp/png) instead of the SVG, attach the high-resolution image or say "use chat image" and I'll optimize and add it.
